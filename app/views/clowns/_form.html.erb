<%= form_for(@clown) do |f| %>
  <% if @clown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clown.errors.count, "error") %> prohibited this clown from being saved:</h2>

      <ul>
      <% @clown.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
